#include <stdio.h>
#include <stdlib.h>
#include "lists.h"
/**
 * insert_node - function that inserts a number into
 *	a sorted singly linked list,
 * @head: pointer,
 * @number: integer,
 * Return: address of node, or NULL if it failed,
 */
listint_t *insert_node(listint_t **head, int number)
{}
